<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS Syntax</title>
</head>
<body>
    <h1><%= title%></h1>
    <p>Current second: <%= seconds%></p>
    <% if (items.length === 0) { %>
        <p>No item to display.</p>
    <% } else { %>
        <ul>
            <% for (let i = 0; i < items.length; i++) { %>
                <li>
                    <%= items[i] %>
                </li>
            <% } %>
        </ul>
    <% } %>

    <%- htmlContent %>

    <%- include("footer.ejs") %>
</body>
</html>